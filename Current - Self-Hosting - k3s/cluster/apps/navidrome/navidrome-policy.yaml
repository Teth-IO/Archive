---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: navidrome
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: navidrome
  policy:
    semver:
      range: '>=0.0.0'
